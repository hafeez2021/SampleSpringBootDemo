app:
  storage-account:
    connectionString: "some connection String"
  datasource:
    jdbcUrl: jdbc:sqlserver://HAFEEZ_DELL:1433;databaseName=testdb
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: testadmin
    password: ABCD@1234
    initialization-mode: never
    platform: mssql